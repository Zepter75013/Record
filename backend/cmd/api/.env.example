# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=discdb

# Server
HTTP_PORT=8080

# JWT
JWT_SECRET=change_me_to_a_random_secret

# Discogs API
DISCOGS_TOKEN=

# Uploads
UPLOADS_DIR=./uploads

# 📧 Configuration SMTP (Email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_FROM_NAME=Vinyl Manager

# Frontend URL (pour les liens dans les emails)
FRONTEND_URL=http://localhost:5173
