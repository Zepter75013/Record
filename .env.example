# Copier ce fichier en ".env" à côté de docker-compose.yml et renseigner
# les vraies valeurs — ce .env n'est jamais commité (voir .gitignore).

DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=discdb

JWT_SECRET=

DISCOGS_TOKEN=

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_FROM_NAME=Vinyl Manager

FRONTEND_URL=
